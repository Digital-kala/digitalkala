export type PartnerProps = {
  name: string;
  logo: string;
  websiteUrl?: string;
}

export type ProfileProps = {
  name: string;
  role: string;
  imageUrl: string;
  linkedinUrl?: string;
}

export type ProgramProps = {
  name: string;
  description: string;
  image?: string;
}